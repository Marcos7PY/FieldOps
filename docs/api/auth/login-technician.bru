meta {
  name: Iniciar Sesión (Técnico)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "tecnico1",
    "password": "Demo2026!"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", res.body.accessToken);
    bru.setEnvVar("refreshToken", res.body.refreshToken);
  }
}