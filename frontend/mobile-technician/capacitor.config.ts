import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fieldops.technician',
  appName: 'mobile-technician',
  webDir: 'www'
};

export default config;
