spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false
    properties:
      schema.registry.url: mock://test-schema-registry
  mail:
    host: localhost
    port: 1025
