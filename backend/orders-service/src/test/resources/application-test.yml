spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: localhost:9092
    properties:
      schema.registry.url: mock://test-schema-registry
    producer:
      compression-type: none

fieldops:
  outbox:
    publisher:
      enabled: false